# Homepage

We should have a home page that doesn't start codex immediately on initial boot. Instead it gives me the option of where to boot (stores previous homes we've used) so I don't have to launch with an env var every time.

Once booted it should give an overview of everything in an easy to read / query way, projects, default settings (skills and mcps) etc...
